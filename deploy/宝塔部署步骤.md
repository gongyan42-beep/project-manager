# 🚀 项目管理器 - 宝塔部署步骤

## 步骤 1：本地打包

```bash
cd /Users/gusuping/code/project-manager
./build.sh
```

## 步骤 2：上传到服务器

```bash
scp -i ~/.ssh/baota_server_key deploy/project-manager.tar.gz root@118.25.13.91:/www/wwwroot/
```

## 步骤 3：服务器部署

```bash
# SSH 登录服务器
ssh -i ~/.ssh/baota_server_key root@118.25.13.91

# 创建项目目录
cd /www/wwwroot
mkdir -p project-manager
cd project-manager

# 解压项目
tar -xzf ../project-manager.tar.gz

# 配置环境变量
# .env 文件已包含，确认配置正确

# Docker 部署
docker-compose up -d --build
```

## 步骤 4：宝塔面板配置

1. 登录宝塔面板：http://118.25.13.91:8888/
   - 用户名：junday
   - 密码：ryhd8888

2. **添加站点**
   - 点击【网站】→【添加站点】
   - 域名：`manager.longgonghuohuo.com`
   - 根目录：`/www/wwwroot/project-manager`
   - PHP版本：纯静态

3. **配置反向代理**
   - 点击站点【设置】→【反向代理】
   - 代理名称：project-manager
   - 目标URL：`http://127.0.0.1:3003`
   - 发送域名：`$host`
   - 点击【提交】

4. **申请 SSL 证书**
   - 点击站点【设置】→【SSL】
   - 选择【Let's Encrypt】
   - 点击【申请】

5. **完成！**
   访问：https://manager.longgonghuohuo.com

## 注意事项

- 确保服务器 3003 端口未被占用
- 确保 Docker 和 docker-compose 已安装
- 检查防火墙是否开放 3003 端口

## 故障排查

### 无法访问
```bash
# 检查 Docker 容器状态
docker ps | grep project-manager

# 查看日志
docker logs project-manager

# 重启容器
docker-compose restart
```

### 端口冲突
```bash
# 查看端口占用
lsof -i :3003

# 修改 .env 中的 PORT 和 docker-compose.yml 中的端口映射
```
